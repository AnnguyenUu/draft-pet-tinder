# TheDogAPI key, used server-side by the Vite dev proxy for /api/v1/* requests
# (breeds, votes). Get a free key at https://www.thedogapi.com/signup.
API_KEY=

# TheDogAPI's base URL (no trailing /v1 — the proxy appends it). Normally:
VITE_API_BASE_URL=https://api.thedogapi.com
